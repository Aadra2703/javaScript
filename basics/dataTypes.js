"use strict";   //treat all JS code as newer version (not mandatory but good practice)

// alert(3 + 3) we are using node js not browser so this will give error

console.log(3+3);

let name = "Aadra"
let age = 20      
let isLoggesdIn = true
let state = null
//null => standalone value  (we are leaving it as empty (empty representation))  // null is object type
// undefined => we have just defined the variable but will assign later it is not null
//symbol =>used for uniqueness

//object 

console.log(typeof null);  //object
console.log(typeof undefined); //undefined
